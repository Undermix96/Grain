services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: undermix/grain-backend:latest

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: undermix/grain-frontend:latest
